node_modules/
dist/
build/
coverage/
.expo/
package-lock.json
apps/app/assets/

# Frozen inputs — controller §0.3 forbids editing these, and reformatting is
# editing. Their bytes are hash-verified against
# docs/specs/BUNKI_SPEC_INTEGRITY_SHA256_2026-07-27.txt.
docs/specs/
docs/convergence/
docs/handoffs/

# Append-only multi-agent evidence (orchestration spec §2.6). Format churn here
# would collide across agents working in parallel.
docs/build-evidence/

# Preserved Sites v11 prototype snapshot — reformatting is editing; its bytes
# are the historical baseline (docs/handoffs/BUNKI_SITES_V11_...HANDOFF).
prototypes/

# Generated Drift-tier data — the generator's compact output IS the format;
# reformatting a 6,692-row lexicon into one-element-per-line helps nobody.
apps/app/src/data/generated/

# Python corpus subtree — its own toolchain; format/test discipline lives in
# corpus CI (pytest), and committed samples are generated data.
corpus/
