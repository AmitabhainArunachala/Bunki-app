node_modules/
dist/
build/
coverage/
.expo/
package-lock.json
apps/app/assets/

# Frozen inputs — controller §0.3 forbids editing these, and reformatting is
# editing. Their bytes are hash-verified against
# docs/specs/BUNKI_SPEC_INTEGRITY_SHA256_2026-07-27.txt.
docs/specs/
docs/convergence/
docs/handoffs/

# Append-only multi-agent evidence (orchestration spec §2.6). Format churn here
# would collide across agents working in parallel.
docs/build-evidence/

# Preserved Sites v11 prototype snapshot — reformatting is editing; its bytes
# are the historical baseline (docs/handoffs/BUNKI_SITES_V11_...HANDOFF).
prototypes/

# Generated Drift-tier data — the generator's compact output IS the format;
# reformatting a 6,692-row lexicon into one-element-per-line helps nobody.
apps/app/src/data/generated/

# Python corpus subtree — its own toolchain; format/test discipline lives in
# corpus CI (pytest), and committed samples are generated data.
corpus/

# Verifier output — the corridor's evidence file is written by
# prototypes/corridor/tools/verify-corridor.mjs on every run. Reformatting it
# would be overwritten by the next run and would churn the diff for nothing.
docs/prototype/verification-report.json

# Local Python venv for the corpus grader runtime (never committed)
.venv/

# Consistency-sweep output — written by verify-drift-consistency.mjs each run
docs/audits/drift-consistency-report.json
docs/audits/drift-consistency-shots/

# Feed-loop artifacts — written by prototypes/corridor/tools/feed_ingest.py on
# every run (and the queue again by feed_apply_review.py). The generator's
# bytes ARE the format: reformatting them would desync the sha256s recorded in
# docs/build-evidence/renkan/feed/ run logs. feed-titles-en.json stays
# prettier-formatted — it is hand-authored, not generated.
docs/content/feed-curation-report.json
docs/content/feed-review-queue.json
