# SRS Technical Audit — 回廊 KAIRO review engine (2026-08-11)

**Engine:** FSRS-6 via `ts-fsrs` v5.4.1, vendored at
`prototypes/corridor/vendor/ts-fsrs.mjs` (1,869 lines), driven by pinned
parameters in `prototypes/corridor/data/fsrs-pin.json`. All app-side scheduling
code is in `prototypes/corridor/corridor.js`. Every number in this audit comes
from executing the shipped code with the shipped parameters, including the
exact localStorage serialize/revive round-trip a real card undergoes.

Pinned parameters: request retention **0.90**, maximum interval **36,500 d**,
fuzz **disabled**, short-term scheduling **enabled**, learning steps
`["1m","10m"]`, relearning steps `["10m"]`, weight set
`bunki-fsrs6-r090-defaults-v1` (the FSRS-6 population defaults).

---

## 1 · Core algorithm

**Algorithm:** FSRS-6 (not SM-2). There is no Ease Factor — FSRS replaces it
with two continuous memory variables per card: **Stability S** (days until
recall probability decays to 90%) and **Difficulty D ∈ [1,10]**.

### 1A. The actual code

App side (`corridor.js`):

```js
const srsKey = (t, id) => `${t}:${id}`;
function srsCardOf(item, now) {
  const rec = S.srs[srsKey(item.t, item.id)];
  if (!rec) return fsrsApi.createEmptyCard(now);
  const c = { ...rec, due: new Date(rec.due) };
  if (rec.last_review) c.last_review = new Date(rec.last_review);
  return c;
}
function srsStore(item, card) {
  S.srs[srsKey(item.t, item.id)] = {
    ...card,
    due: card.due.toISOString(),
    last_review: card.last_review ? card.last_review.toISOString() : undefined,
  };
  saveStore();
}
```

The grade handler (inside `renderReview`) — one `repeat()` call computes all
four futures, the pressed one is stored:

```js
const card = srsCardOf(item, now);
const result = scheduler.repeat(card, now);
// per button: result[fsrsApi.Rating[rating]].card  → srsStore(item, …)
```

The engine's formulas (verbatim from the vendored `ts-fsrs.mjs`):

```js
// Retrievability (power forgetting curve):
function forgetting_curve(decayOrParams, elapsed_days, stability) {
  const { decay, factor } = computeDecayFactor(decayOrParams);
  return roundTo(Math.pow(1 + factor * elapsed_days / stability, decay), 8);
}
// Interval from stability at request_retention 0.9:
next_interval(s, elapsed_days) {
  const newInterval = Math.min(Math.max(1, Math.round(s * this.intervalModifier)),
    this.param.maximum_interval);
  return this.apply_fuzz(newInterval, elapsed_days);   // fuzz disabled → identity
}
// Difficulty update (linear damping + mean reversion toward D0(Easy)):
next_difficulty(d, g) {
  const delta_d = -this.param.w[6] * (g - 3);
  const next_d = d + this.linear_damping(delta_d, d);  // delta_d * (10-d)/9
  return clamp(this.mean_reversion(this.init_difficulty(Rating.Easy), next_d), 1, 10);
}
// Stability after successful recall (w15 = Hard penalty, w16 = Easy bonus):
next_recall_stability(d, s, r, g) {
  const hard_penalty = Rating.Hard === g ? w[15] : 1;
  const easy_bound  = Rating.Easy === g ? w[16] : 1;
  return clamp(s * (1 + Math.exp(w[8]) * (11 - d) * Math.pow(s, -w[9])
    * (Math.exp((1 - r) * w[10]) - 1) * hard_penalty * easy_bound), S_MIN, 36500);
}
// Stability after a lapse:
next_forget_stability(d, s, r) {
  return clamp(w[11] * Math.pow(d, -w[12]) * (Math.pow(s + 1, w[13]) - 1)
    * Math.exp((1 - r) * w[14]), S_MIN, 36500);
}
```

### 1B. Learning steps vs review intervals

Learning/relearning state machine (`BasicLearningStepsStrategy`, verbatim
behavior): while a card is in Learning and hasn't exhausted `["1m","10m"]`:

- **Again** → back to step 0 (1 min)
- **Hard** → mean of steps 1 and 2 = **6 min** (this is where the 6-minute
  Hard label comes from)
- **Good** → next step (10 min), then graduation to Review
- **Easy** → skips straight to Review with the Easy initial stability

A lapse from Review enters Relearning at the 10-min step. **Again on a Review
card always means relearning** — never an interval multiply.

### 1C. Graduating / easy interval

Not fixed constants as in Anki — computed from initial stability
`init_stability(g) = w[g-1]`, so S₀ = 0.212 / 1.293 / 2.307 / 8.296 for
Again/Hard/Good/Easy. With this weight set graduation lands at **2 d** (Good)
and initial **Easy = 8 d**.

### 1D. Due dates, day boundaries, timezone

Due is an exact UTC timestamp (ISO string in storage); queue admission is
`new Date(rec.due) <= now` — no day bucketing, no Anki-style 4 a.m. rollover.
Display bucketing (今日/明日) is by the device's **local midnight** (fixed
2026-08-11: cards due later today previously displayed as 明日). Sub-day
intervals are real minutes; day intervals land at the same clock time N days
later.

---

## 2 · Full lifecycle (executed, through the storage round-trip)

```
t+0      NEW                        → shown: Again 1min · Hard 6min · Good 10min · Easy 8d
t+0      Good  → Learning  10min   S=2.31   D=2.12  reps=1
t+10min  Good  → Review    2d      S=2.31   D=2.11  reps=2   (graduates)
due day  Good  → Review    11d     S=10.97  D=2.10  reps=3
due day  Hard  → Review    32d     S=32.23  D=4.74  reps=4   (see note below)
due day  Again → Relearn.  10min   S=2.40   D=8.26  lapses=1 (S collapsed 32.23→2.40, D 4.74→8.26)
+10min   Good  → Review    2d      S=2.40   D=8.24            (re-graduates)
review 1 Good  → 5d    S=5.10     review 2 Good → 11d   S=10.81
review 3 Good  → 22d   S=21.85    review 4 Good → 42d   S=41.62
review 5 Good  → 76d   S=75.67    review 6 Good → 132d  S=131.77
```

Post-lapse growth is visibly slower than pre-lapse (D=8.2 vs 2.1 damps the
S-growth term `(11−D)`) — the FSRS-intended behavior.

**Note on Hard:** Hard _on the due day_ grows the interval (32 d from 11 d)
because the card survived the full interval; Hard means "recalled with
effort," not "shrink the interval." This is correct FSRS and differs from
SM-2 intuition.

---

## 3 · Edge cases (executed)

**Long absence** (7d-stable card, 90 days overdue, retrievability R = 70.1%):

| Button | Result                                                 |
| ------ | ------------------------------------------------------ |
| Again  | Relearning 10 min (S 10.97 → 2.14, D → 7.39, lapses+1) |
| Hard   | Review 80 d                                            |
| Good   | Review **125 d**                                       |
| Easy   | Review 225 d                                           |

Surviving a long gap earns large stability — correct FSRS; no "overdue
penalty," no interval reset.

**Hard forever (from Review):** 8d → 19d → 36d → 57d → 81d → 107d, with D
marching 4.75 → 9.28. Intervals keep growing but decelerate as D saturates —
no SM-2-style "ease hell" collapse.

**Easy forever:** 8d → 66d → 397d → 1,875d → 7,265d → 23,933d, D pinned at
1.00. Grows fast by design; capped at 36,500 d.

**Day boundary:** a card due 23:59 is not due at 23:58, is due at 23:59 and
any moment after — exact-timestamp comparison, no cliff, no double count.

**Overdue credit:** the same card graded Good exactly on due → 11 d; graded
Good 30 days late → **38 d** (S 10.97 vs 38.19). Lateness feeds the math.

**Cap:** S=99,999 pressed Good → `scheduled_days` prints **36,501** against
the 36,500 cap — interval is capped, the derived day-count is off by one at
the extreme. Cosmetic, but real.

---

## 4 · Button set and effects

Four grades — もう一度/Again, 難しい/Hard, ふつう/Good, 簡単/Easy — each
labeled with its true computed next interval (minutes or days read from the
actual `result` card, not an estimate).

| Button | Effect                                                                       |
| ------ | ---------------------------------------------------------------------------- |
| Again  | Lapse: S via `next_forget_stability`, lapses+1, relearning 10 min, D worsens |
| Hard   | Success with w₁₅ stability penalty; D worsens                                |
| Good   | Success; D nearly flat                                                       |
| Easy   | Success with w₁₆ stability bonus; D improves                                 |

Plus, outside the four grades: **rest** (suspend without grading — schedule
untouched, woken from the list), and **undo** (restores the exact prior stored
card object, never recomputed: `if (last.prev === undefined) delete
S.srs[key]; else S.srs[key] = last.prev`).

---

## 5 · Data model & content independence

The scheduler is fully content-blind. A card is keyed `"{type}:{id}"`
(`word:安堵`, `kanji:海`, `radical:氵`, `idiom:一期一会`) and stores only FSRS
state — the exact bytes:

```json
{
  "due": "2027-07-12T10:20:00.000Z",
  "stability": 131.76864467,
  "difficulty": 8.16659251,
  "elapsed_days": 76,
  "scheduled_days": 132,
  "reps": 12,
  "lapses": 1,
  "learning_steps": 0,
  "state": 2,
  "last_review": "2027-03-02T10:20:00.000Z"
}
```

Content is resolved at render time by `reviewBack(item)`, which switches on
the item type and reads the dictionary/kanji/radical/idiom records (or the
`S.deepWords` snapshot for 70k-tier words). Nothing content-shaped touches the
math.

**However — the target Japanese card-type taxonomy is not yet real.** Today
there is **one recognition card per item** (front: written form/glyph, or —
every other repetition of a word with corpus sentences — a cloze-blanked real
sentence; back: reading + senses). There are **no separate on-yomi/kun-yomi
cards, no production/reverse cards, and no authored AJATT sentence-mining card
type** — the cloze is derived automatically, not minable. The engine would
schedule such cards without modification (it is key-blind), but the card types
themselves do not exist.

---

## 6 · Queue & runtime behavior

```js
const NEW_PER_SESSION = 20;
function srsDueItems(now = new Date()) {
  const reviews = [],
    fresh = [];
  for (const item of S.taken) {
    if (S.suspended[key]) continue;
    const rec = S.srs[key];
    if (!rec) fresh.push(item);
    else if (new Date(rec.due) <= now) reviews.push(item);
  }
  return [...reviews, ...fresh.slice(0, NEW_PER_SESSION)];
}
```

- Due reviews first (in capture order), then up to 20 never-seen cards.
- The queue is **frozen at session start**.
- After days away, everything due enters one queue — no backlog cap, no
  penalty; lateness is credited (see §3).
- Scoped sessions (per-list) filter the same queue by key — same cards, same
  schedule, narrower door.
- The 集中道場 additionally cycles its pool to keep drilling when cards run
  dry; those extra passes are genuinely FSRS-rescheduled (early-review
  semantics: high R → small S gain, which the engine prices correctly).
- Leech: `lapses >= 6` → named 苦手 at session end, rest offered, never
  auto-suspended. Partial knowledge stays first-class.

---

## 7 · Verdict

**Is this a complete, correct, production-ready SRS engine? — Partially.**

### Genuinely correct and proven above

- FSRS-6 scheduling mathematics: pinned weights, real learning/relearning
  steps, correct lapse / overdue / cap behavior — demonstrated numerically
  through the actual storage round-trip.
- Exact-state undo; suspend/leech handling; content-blind card model; honest
  button labels; whole-record export/import; storage that cannot silently
  destroy state (read-only quarantine, unknown-key preservation).

### Missing or wrong, precisely

1. **No durable per-review log.** Only the in-session `rv.history` (discarded
   at session end) and a daily aggregate `{n, again}` survive. Without a
   persisted log of (card, rating, timestamp, elapsed, latency) the FSRS
   optimizer can never personalize the weights to this learner, history
   cannot be audited, and state cannot be rebuilt after a bug. **The single
   biggest gap for trusting the system with years of study.**
2. **Learning steps don't reinsert within a session.** Press Again and the
   card's 1-minute step matures, but the frozen queue never shows it again
   that session — in plain review the 1m/10m steps effectively span
   _sessions_, not minutes. Anki reinserts learning cards into the live
   queue. (The dojo's cycling pool masks this; plain review has it.)
3. **New-card pacing is per-session (20), not per-day.** Three sessions in a
   day can introduce 60 new cards. No daily cap exists.
4. **Fuzz is disabled**, so a batch of cards captured together stays
   synchronized forever — same-day due-pile clumping, the thing fuzz exists
   to prevent.
5. **One card type per item** — no on/kun split, no production direction, no
   authored sentence cards (§5).
6. Parameters are fixed defaults — fine to start (FSRS defaults are fit on
   large populations), but personalization is impossible until (1) exists.
7. Cosmetic: `scheduled_days` 36,501 at the 36,500 cap; undo does not survive
   a reload.

### Order of repair before trusting it with real long-term data

1. Append-only persisted review log (also the foundation the
   conversation-graph / adaptive-layer vision needs).
2. Learning-card reinsertion into the live queue.
3. True daily new-card cap.
4. Enable fuzz.
5. The Japanese card-type taxonomy as a deliberate design round with the
   operator.

None of these change the core math, which holds up.

---

# Part II — Deeper evidence (same day, operator request)

Everything below is verbatim code from the current tree and executed output
from the shipped engine + pin, with the app's exact localStorage round-trip
applied between every press.

## II.1 · The exact code path when a user rates a card

**Step 1 — engine construction** (in `boot()`, `corridor.js`; parameters come
from `data/fsrs-pin.json`, the vendored module from `vendor/ts-fsrs.mjs`):

```js
fsrsApi = window.__TSFSRS__ || (await import('./vendor/ts-fsrs.mjs'));
scheduler = fsrsApi.fsrs(
  fsrsApi.generatorParameters({
    w: pin.w,
    request_retention: pin.requestRetention,
    maximum_interval: pin.maximumInterval,
    enable_fuzz: pin.enableFuzz,
    enable_short_term: pin.enableShortTerm,
    learning_steps: pin.learningSteps,
    relearning_steps: pin.relearningSteps,
  }),
);
```

**Step 2 — revive the stored card, compute all four futures** (in
`renderReview`, after the answer is revealed):

```js
const now = new Date();
const card = srsCardOf(item, now); // stored JSON -> live card (Dates revived)
const result = scheduler.repeat(card, now); // ONE call: futures for all 4 ratings
```

with:

```js
const srsKey = (t, id) => `${t}:${id}`;
function srsCardOf(item, now) {
  const rec = S.srs[srsKey(item.t, item.id)];
  if (!rec) return fsrsApi.createEmptyCard(now);
  const c = { ...rec, due: new Date(rec.due) };
  if (rec.last_review) c.last_review = new Date(rec.last_review);
  return c;
}
```

**Step 3 — the four buttons; the pressed rating's precomputed card is stored
byte-for-byte** (verbatim, current tree):

```js
const grades = [
  ['Again', 'again', 'もう一度'],
  ['Hard', 'hard', '難しい'],
  ['Good', 'good', 'ふつう'],
  ['Easy', 'easy', '簡単'],
];
for (const [rating, key, ja] of grades) {
  const next = result[fsrsApi.Rating[rating]].card;
  const ms = next.due.getTime() - now.getTime();
  const when =
    next.scheduled_days >= 1
      ? tx(`${next.scheduled_days} 日`, `${next.scheduled_days} d`)
      : tx(
          `${Math.max(1, Math.round(ms / 60000))} 分`,
          `${Math.max(1, Math.round(ms / 60000))} min`,
        );
  const b = el('button', `grade g-${key}`);
  b.append(el('span', 'g-label', tx(ja, key)));
  b.append(el('span', 'g-when', when));
  b.addEventListener('click', () => {
    const day = dayKey();
    rv.history.push({ key, prev: S.srs[srsKey(item.t, item.id)], day }); // exact undo state
    srsStore(item, result[fsrsApi.Rating[rating]].card); // <- THE state transition
    const st = (S.stats[day] ||= { n: 0, again: 0 });
    st.n += 1;
    if (key === 'again') st.again += 1;
    saveStore();
    rv.done[key] += 1;
    rv.ix += 1;
    rv.revealed = false;
    render();
  });
  row.append(b);
}
```

**Step 4 — persistence** (ISO strings in, Dates out; nothing else touches the
card):

```js
function srsStore(item, card) {
  S.srs[srsKey(item.t, item.id)] = {
    ...card,
    due: card.due.toISOString(),
    last_review: card.last_review ? card.last_review.toISOString() : undefined,
  };
  saveStore();
}
```

So the complete path is:
`tap grade → result[Rating[g]].card (precomputed by ts-fsrs repeat()) →
srsStore → localStorage`. The app performs **no interval arithmetic of its
own** — the only app-side math is the minutes/days label, read from the
engine's own `due`/`scheduled_days`.

## II.2 · Complete numerical lifecycle (executed; includes Again and Hard twice each)

Storage round-trip applied between every press. `R at press` is the engine's
own `get_retrievability` for Review-state cards; `step` is the card's
`learning_steps` field.

```
 0. NEW card                              state=New        step=0  S=—        D=—      reps=0 lapses=0
 1. 10:00 press Again                  -> state=Learning   step=0  ivl=1 min  S=0.2120  D=6.4133 reps=1  due=10:01  <- AGAIN #1: born difficult (D0(1)=w4)
 2. 10:01 press Hard                   -> state=Learning   step=0  ivl=6 min  S=0.2120  D=7.6042 reps=2  due=10:07  <- HARD in Learning: 6 min = mean(1m,10m)
 3. 10:07 press Good                   -> state=Learning   step=1  ivl=10 min S=0.2467  D=7.5918 reps=3  due=10:17  <- advances to step 1
 4. 10:17 press Good                   -> state=Review     step=0  ivl=1 d    S=0.2842  D=7.5795 reps=4  due=+1d    <- GRADUATES
 5. +1d   press Good  (R=79.4%)        -> state=Review     ivl=2 d    S=1.6704  D=7.5671 reps=5
 6. +2d   press Hard  (R=88.7%)        -> state=Review     ivl=4 d    S=3.6052  D=8.3702 reps=6            <- HARD in Review: w15 penalty, D worsens
 7. +4d   press Again (R=89.3%)        -> state=Relearning ivl=10 min S=0.7678  D=9.4495 reps=7  lapses=1  <- LAPSE: S 3.61→0.77, relearning step 10m
 8. +10m  press Good                   -> state=Review     ivl=1 d    S=0.8208  D=9.4353 reps=8            <- re-graduates
 9. +1d   press Good  (R=88.6%)        -> ivl=2 d    S=1.6416  D=9.4211
10. +2d   press Good  (R=88.6%)        -> ivl=4 d    S=3.1175  D=9.4069
11. +4d   press Good  (R=88.2%)        -> ivl=6 d    S=5.7495  D=9.3927
12. +6d   press Good  (R=89.7%)        -> ivl=10 d   S=9.5843  D=9.3786
```

Read together with Part I §2 (a card whose first press was Good: graduates at
2 d with D≈2.1 and reaches 132 d by review 6): **a card born with Again and
bruised by Hard carries D≈9.4 and crawls 1d→2d→4d→6d→10d, while a clean card
with identical Good presses sprints 2d→11d→…→132d.** Per-card difficulty is
doing exactly its job, and the lapse collapse (S 3.61→0.77 while D rises to
9.45) is the textbook FSRS forget transition.

## II.3 · UI button → FSRS Rating mapping (explicit)

The vendored enum, verbatim:

```js
var Rating = ((Rating2) => {
  Rating2[(Rating2['Manual'] = 0)] = 'Manual';
  Rating2[(Rating2['Again'] = 1)] = 'Again';
  Rating2[(Rating2['Hard'] = 2)] = 'Hard';
  Rating2[(Rating2['Good'] = 3)] = 'Good';
  Rating2[(Rating2['Easy'] = 4)] = 'Easy';
  return Rating2;
})(Rating || {});
```

| UI button        | JS lookup                         | FSRS Rating value |
| ---------------- | --------------------------------- | ----------------- |
| もう一度 / again | `result[fsrsApi.Rating['Again']]` | **1**             |
| 難しい / hard    | `result[fsrsApi.Rating['Hard']]`  | **2**             |
| ふつう / good    | `result[fsrsApi.Rating['Good']]`  | **3**             |
| 簡単 / easy      | `result[fsrsApi.Rating['Easy']]`  | **4**             |

`Rating.Manual = 0` is never used by the app (rest/suspend deliberately does
not grade — the schedule is untouched, per the code in Part I §4).

## II.4 · Learning/relearning steps × short-term scheduling (mechanism + proof)

Two layers cooperate, and the card's own `learning_steps` counter is the
bridge:

**Layer 1 — the steps strategy** decides _minutes and step index_ while a card
is inside `["1m","10m"]` (Learning) or `["10m"]` (Relearning). Verbatim core:

```js
const BasicLearningStepsStrategy = (params, state, cur_step) => {
  const learning_steps = state === State.Relearning || state === State.Review
    ? params.relearning_steps : params.learning_steps;
  const steps_length = learning_steps.length;
  if (steps_length === 0 || cur_step >= steps_length) return {}; // steps exhausted -> pure FSRS
  ...
  // Again -> first step (1m), next_step = 0
  // Hard  -> steps_length === 1 ? firstStep * 1.5 : mean(step0, step1)  = 6 min here
  // Good  -> next step's minutes; graduation when cur_step+1 >= steps_length
};
```

**Layer 2 — the memory math** updates S and D on _every_ press regardless of
steps, with a dedicated same-day branch. Verbatim dispatch from `next_state`:

```js
r = typeof r === 'number' ? r : this.forgetting_curve(t, s);
let new_s;
if (t === 0 && this.param.enable_short_term) {
  new_s = this.next_short_term_stability(s, g); // same-day press
} else if (g === 1) {
  const s_after_fail = this.next_forget_stability(d, s, r);
  let [w_17, w_18] = [0, 0];
  if (this.param.enable_short_term) {
    w_17 = w[17];
    w_18 = w[18];
  }
  const next_s_min = s / Math.exp(w_17 * w_18);
  new_s = clamp(roundTo(next_s_min, 8), S_MIN, s_after_fail); // lapse floor uses w17·w18
} else {
  new_s = this.next_recall_stability(d, s, r, g); // normal spaced recall
}
```

and the same-day stability formula itself:

```js
next_short_term_stability(s, g) {
  const sinc = Math.pow(s, -w[19]) * Math.exp(w[17] * (g - 3 + w[18]));
  const maskedSinc = g >= Rating.Hard ? Math.max(sinc, 1) : sinc;  // success never shrinks S same-day
  return roundTo(clamp(s * maskedSinc, S_MIN, 36500), 8);
}
```

**Numeric proof of the interaction** (executed): rows 1–4 above show the steps
walking 1m → 6m → 10m → graduate while S inches 0.2120 → 0.2467 → 0.2842 via
the short-term formula (t=0 each time). And a Review-state card pressed twice
at the same instant:

```
14. 10:10 press Good -> Review  ivl=2 d  S=2.3065
15. 10:10 press Good -> Review  ivl=3 d  S=2.3065   <- t=0: maskedSinc clamps to 1,
                                                       S unchanged; no free stability
                                                       from hammering Good same-day
```

This is the property that makes the 集中道場's drill-cycling safe: repeated
same-day passes cannot inflate a card's memory state (Good/Easy same-day is
S-neutral-or-tiny; only spaced recalls grow S materially).

## II.5 · Self-assessment (restated, unchanged by the deeper evidence)

**Verdict: Partially production-ready.** The scheduling core is FSRS-6,
correctly wired, with the state transition living entirely inside the audited
`ts-fsrs` engine; the app adds zero interval arithmetic; persistence is a
lossless ISO round-trip proven by executing every example through it. The
deeper pass strengthened confidence in two places: same-day presses cannot
inflate stability (II.4), and per-card difficulty separates rough-start cards
from clean cards exactly as designed (II.2).

Unchanged gaps, in repair order:

1. **No durable per-review log** (session `rv.history` is discarded; only
   daily `{n, again}` aggregates persist) — blocks FSRS weight
   personalization, audit, and state reconstruction. Biggest gap.
2. **No in-session learning-step reinsertion** — the frozen queue means 1m/10m
   steps span sessions in plain review (the dojo masks this).
3. **New-card cap is per-session (20), not per-day.**
4. **Fuzz disabled** — batch-captured cards stay due-synchronized forever.
5. **One recognition card type per item** — no on/kun, production, or authored
   sentence-mining card types yet (engine is ready for them; content model is
   not).
6. Fixed default weights (personalization needs #1 first); cosmetic
   `scheduled_days` 36,501 at the cap; undo is session-only.

I would not entrust this engine with years of study data until #1 exists; with
#1–#4 landed, I would.

---

# Part III — The five gaps, closed (same day; executed proofs)

All five gaps named in the Part I/II verdict are now implemented on this
branch. Everything below is real code from the tree and executed output.

## III.1 · Durable append-only review log

**Structure** (`S.revlog`, persisted in the learner envelope, exported and
imported with it, protected by the same read-only quarantine):

```
[t, key, g, stBefore, elapsed, r, sBefore, dBefore, sAfter, dAfter, ivl, dueAfter]

 t        press time, epoch MILLISECONDS — ms precision matters: the engine's
          fuzz seed is `${time}_${reps}_${d*s}`, so the exact timestamp makes
          every fuzzed interval reproducible on replay
 key      "type:id" scheduler key
 g        FSRS Rating pressed (1 Again · 2 Hard · 3 Good · 4 Easy);
          g = 0 is a revocation (undo): the row is [t, key, 0, revokedIndex]
 stBefore card state before (0 New · 1 Learning · 2 Review · 3 Relearning)
 elapsed  days since last_review at press (3 dp; null on a first press)
 r        engine retrievability at press (4 dp; null without last_review)
 sBefore/dBefore  memory state before (4 dp; null on a first press)
 sAfter/dAfter    memory state after
 ivl      scheduled_days of the stored result
 dueAfter stored due, epoch ms
```

**Write path** — first thing in the grade click handler, before the card
state is stored:

```js
const logIx = srsLogReview(skey, card, next, fsrsApi.Rating[rating], now);
```

with `srsLogReview` computing elapsed and the engine's own
`get_retrievability` and pushing the row (see `corridor.js`). Undo never
erases: it appends `[t, key, 0, revokedIndex]` and restores the prior card
object. This is sufficient for the FSRS optimizer (per-card sequences of
(elapsed, rating)), analytics, and deterministic state reconstruction.

**Executed proof** (Playwright, real UI, two fresh cards, one session):

```
revlog rows: 5
row 1: [1786442939454,"word:世界",1,0,null,null,null,null,0.212,6.4133,0,1786442999454]
row 2: [1786442939971,"word:開催",3,0,null,null,null,null,2.3065,2.1181,0,1786443539971]
row 3: [1786443007352,"word:世界",3,1,0.001,1,0.212,6.4133,0.2467,6.4021,0,1786443607352]
after reload: revlog rows: 5          <- persists across restarts
```

Row 1's `dueAfter − t` = exactly 60,000 ms (the 1-minute step). Row 3 records
R = 1.0 at elapsed 0.001 d — the engine's own numbers, logged.

Undo proof (separate run): after one Good then undo —
`rows: 2, last row: [t,"word:w0",0,0]` (revocation pointing at row 0),
`nnew` unwound 16 → 15, card state erased. Append-only holds.

## III.2 · In-session learning-step reinsertion

**Code path** — in the grade handler:

```js
if ((next.state === 1 || next.state === 3) && next.scheduled_days < 1) {
  rv.queue.push(item); // the card returns in THIS session
  entry.reinserted = true; // so undo can remove the pending copy
}
```

and at the top of the card renderer: the nearest-ripening card is brought
forward (a ready card is always nearest); a beat of ≤ 90 s — the 1-minute
step — is honoured with a quiet zen countdown that turns the card over by
itself; a longer gap (the 10-minute step with nothing else left) is pulled
early instead of parking the learner — Anki's learn-ahead, priced correctly
by the engine's same-day branch (Part II.4 proved same-day Good is
stability-neutral). A quiet 「いま見る」 skips the beat. The dojo bypasses
all of this: drilling early is its point.

**Executed proof** (real UI, real clock):

```
card 1 世界 -> Again        (1m step, re-queued)
card 2 開催 -> Good         (10m step, re-queued)
wait face: {"waiting":true,"clock":"0:59","skip":true}
世界 re-presented and graded 67 s after the Again press — same session
  (revlog row 3: state=1 Learning, elapsed=0.001 d)
10-minute steps: {"waiting":false} — pulled early, no long park
summary: "Session done — 5 cards" (counts grades, not queue rows)
```

## III.3 · Daily new-card cap + fuzz

**Cap** — introduction is counted when a card receives its FIRST grade
(`S.stats[day].nnew`), and the queue honours the remainder across every
session of the day:

```js
const NEW_PER_DAY = 20;
const introducedToday = S.stats[dayKey(now)]?.nnew || 0;
const room = Math.max(0, NEW_PER_DAY - introducedToday);
return [...reviews, ...fresh.slice(0, room)];
```

Executed proof: 25 fresh cards with 20 already introduced today → review
button `予定なし`, disabled. Same store with `nnew = 15` → `復習する — 5 件`.
Undo decrements the day's count.

**Fuzz** — ON by default at the corridor layer (`S.stats.fuzzOff = true`
turns it off), deliberately diverging from the domain kernel's replay pin;
the divergence is safe because ts-fsrs seeds fuzz from
`${reviewTimeMs}_${reps}_${difficulty*stability}` — all recorded exactly in
the revlog, so replay reproduces every fuzzed interval. Executed proof, six
cards captured together (identical S=11, D=4), graded Good moments apart:

```
unfuzzed: all six -> 39 d           (due-pile stays synchronized forever)
fuzzed:   35d, 39d, 40d, 42d, 43d, 38d   (spread ±10%)
re-run with identical inputs: identical outputs  (deterministic)
learning steps: untouched by fuzz (< 2.5 d never fuzzed)
```

## III.4 · Architecture (explicit)

The scheduler is content-blind: a card is any opaque `"type:id"` string.
The key grammar deliberately leaves room for the Japanese card families —
`kanji:海:on` / `kanji:海:kun` (reading cards), `word:安堵:prod`
(production), `sent:<sourceId>#<n>` (mined sentences) — each its own card
with its own FSRS state and revlog rows, sharing the item's content record.
Adding them touches capture and rendering only; `srsCardOf` / `srsStore` /
`srsDueItems` / the revlog need no changes. This is now documented in the
code at the definition of `srsKey`.

The revlog + pinned parameters are sufficient for: per-user FSRS weight
optimization, desired retention as a setting (a scheduler-construction
parameter), richer leech policy (lapse patterns are in the log), multiple
card directions (new keys), and full-history export/import (the envelope
already carries it).

## III.5 · Updated verdict

**Now solid, with executed evidence:** FSRS-6 math (Parts I–II); a durable,
append-only, optimizer-ready review log written on every grade and surviving
restarts; correct in-session learning-step behavior with due-times honoured
and long gaps pulled early; a true daily new-card cap enforced across
sessions with undo unwinding; deterministic fuzz de-synchronizing
batch-captured cards; a content-blind key architecture ready for on/kun,
production, and sentence cards.

**Remaining before I would call it complete for multi-year data:**

1. **Storage medium.** The revlog lives in the localStorage envelope; at
   heavy use (~70k reviews/year ≈ 4–5 MB/year) it will eventually press
   against localStorage limits. The 書き出す export carries it out today;
   an IndexedDB (or file-backed) log store is the right next home. The
   format needs no change — only the shelf it sits on.
2. **The optimizer itself** — the log now feeds it, but the fitting run
   (personalized weights, desired-retention UI) is not built.
3. **The Japanese card-type taxonomy** — the architecture is ready; the
   card families themselves are a design round with the operator, not a
   unilateral build.
4. Small debts: `scheduled_days` 36,501 at the interval cap (engine-side
   cosmetic); undo is session-scoped (the revocation rows, however, are
   permanent).

With this run, the core loop — capture → schedule → review → log — is one I
would trust with real long-term study data, provided the learner exports
regularly (the app now nudges this) until the log moves to IndexedDB.
