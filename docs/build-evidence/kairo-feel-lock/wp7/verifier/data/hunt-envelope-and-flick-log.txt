WP7 verifier — raw hunt log and flick instrumentation.

PRECONDITION, and it matters for every number here:
  c6ed57a does NOT regenerate the corridor fusion. Rebuilding drift-layer.js at
  c6ed57a adds 150 lines (the whole WP7 motion system):
      git checkout c6ed57a && node prototypes/corridor/tools/build-drift-layer.mjs
      git diff --stat -- prototypes/corridor/
      →  prototypes/corridor/drift-layer.js | 157 +++++++++++++++--- (150 insertions)
  verify-drift-hunt and verify-drift-consistency both drive #drift-layer, so an
  unrebuilt run tests WP2, not WP7. Every run below was taken with the fusion
  rebuilt. At pristine 0c6fb07 the committed fusion IS in sync (rebuild → no diff).

SOURCE IDENTITY:
  git diff 49091bd 0c6fb07 -- prototypes/drift/drift-artifact.html  →  EMPTY
  The tree I verified in WP2 round 3 is byte-identical to pristine here.

FIELD IS RANDOMISED:
  drift-artifact.html:673  function shuffle(a){ ... Math.floor(Math.random()*(i+1)) ... }


=== HUNT ENVELOPE (uninstrumented, fusion rebuilt) ===

pristine 0c6fb07:
  run 1 — 3 fails
     FAIL a held finger keeps a constellation alive past the 10s fade — no open water
     FAIL a corpus-backed semantic member is staged, hit-testable DOM interaction
     FAIL hub release cannot hijack a gesture or leave themed ghost satellites
          terminal 372px: centre="増加", deliberate release ctr="増加"→"増加",
          second-tap depth="", after theme+surface centre="増加", sats=14
  run 2 — 2 fails  (semantic member; hub, same signature)
  run 3 — 2 fails  (semantic member; hub, same signature)
  HUB CLUSTER: FAILS 3 of 3.

WP7 c6ed57a:
  run 1 — 3 fails  (flick judgment; semantic member; hub terminal 193px, same signature)
  run 2 — 2 fails  (semantic member; hub)
  run 3 — 5 fails  (flick judgment; held-finger "no open water"; semantic member;
                    kana-only fallback; hub)
  HUB CLUSTER: FAILS 3 of 3.

The failing hub clause in every case is deliberateRelease.ctr === null: a deliberate
tap on a fresh hub with a bloom held did not release the constellation. That is the
ratified full-presence-outranks-hub behaviour confirmed in WP2 round 3
(probe9 case 3: 見舞い op 0.710 on a hub keeps its tap, opens no door).
In round 3 the bloom centre was 外出 on every run and this clause passed; here it is
増加 on every run and it fails. Randomised layout decides it.


=== FLICK INSTRUMENTATION (hunt copy, DOM-only measurements, flick test only) ===

WP7 c6ed57a, fusion:
 FLICK-DIAG {"word":"増加","aim":{"x":239.2,"y":466.5},
   "preRect":{"cx":239.4,"cy":466.7,"w":22,"h":13,"op":0.39,"pe":"auto"},
   "atPressRect":{"cx":240.9,"cy":467.2,"op":0.391,"pe":"auto"},
   "aimGapPx":1.84,"pressInsideWordBox":true,"topElementAtAim":"word:増加",
   "trayPre":"","trayPost":"",
   "heldMsWallClock":478,"downDispatchMs":104,"movesMs":369,
   "FLICK_THRESHOLD_330":"OVER","speedPxPerMs":0.272,"speedGate045":"fail",
   "GRADED":false}
 (an earlier instrumented WP7 run, before timing was added, showed the same:
  aimGapPx 1.9, pressInsideWordBox true, topElementAtAim "word:増加", GRADED false)

pristine 0c6fb07, fusion — THE CONTROL:
 run A  aimGapPx 0.72 · pressInsideWordBox true · topElementAtAim "word:増加"
        heldMsWallClock 415 · OVER · speed 0.313 · GRADED false → flick check FAILED
 run B  aimGapPx 0.58 · pressInsideWordBox true · topElementAtAim "word:増加"
        heldMsWallClock 521 · OVER · speed 0.250 · GRADED false → flick check FAILED

  Pristine 521ms is SLOWER than WP7 478ms. No systematic WP7 penalty.

standalone control (probe11, 12 trials, hunt's own 2300ms settle, WP7 source):
  heldMs 111 149 148 124 145 146 145 103 137 176 138 165   (all << 330)
  aimGapPx 2.28 2.11 1.22 1.14 0.58 0.41 0.61 0.30 0.32 1.50 1.42 1.66
  12 / 12 GRADED.  checkFails 0.  pressAlwaysInsideBox true.

THE GATE:  flick = moved && (held < 330 || |dx|/held > 0.45) && |dy| < 90
  130px delivered over 415-521ms is a slow DRAG. The app refuses to grade it, which
  is correct. The coordinate was never stale — the press landed on the word every
  single time, to within 2px.

CAVEAT ON MY OWN INSTRUMENT: the two extra page.evaluate calls inside the gesture add
latency, so instrumented runs fail on BOTH trees every time. They establish the
MECHANISM. The rates come from the uninstrumented runs above.


=== SUITES ON WP7 (fusion rebuilt) ===
verify-v11                     21/21, zero console, zero page errors
                               census: 36 words · 5 ghosted · minGhostOp 0.327
verify-drift-consistency fast  44/45 · 45/45 · 45/45
                               (the single violation matches the fuzz-sat-reveal
                                misfire proved pre-existing in WP2 round 2 by
                                reproducing it on the f433edf fusion)
build-drift-layer              12/12 anchors, all asserted unique, no adjustment
