# KAIRO feel-lock campaign — baseline ledger (2026-08-09)

Branch `claude/kairo-feel-lock-2026-08-09` cut from `976233d`
(`origin/codex/kairo-full-build-20260808`, verified ancestor).

## Generator parity at base (before any edit)

- `build-drift-layer.mjs`: run twice, both byte-identical to committed
  `drift-layer.css/html/js` (clean `git status` after each run).
- `build-standalone.mjs`: output byte-identical to committed
  `corridor-standalone.html` (8,530,847 bytes) via `cmp`.

## Suite state at base

| Suite | Result |
| --- | --- |
| verify-drift-consistency `--mode fast` (run 1) | 45/45 ok, 0 violations, 0 page errors |
| verify-drift-consistency `--mode fast` (run 2) | 45/45 ok, 0 violations, 0 page errors |
| verify-corridor.mjs | 91/91 ok |
| verify-drift-hunt.mjs (runs 1–3) | 24 ok, **2 pre-existing FAILs** (flaky: run 2 showed only 1) |

### Pre-existing hunt failures at base SHA (NOT introduced by this campaign)

1. `hunt · a corpus-backed semantic member is staged, hit-testable DOM
   interaction` — stage1=false/false, stage2=true/false (staged reveal not
   engaging on first tap).
2. `hunt · a kana-only semantic word grows a meaningful same-level fallback
   constellation` — reading="" and first=false/false on the fallback member
   (intermittent; passed on one of three runs).

Both concern the Drift staged-reveal ladder. Full log:
`baseline-hunt-run3.log`.
