WP6 — verify-drift-hunt raw log, pristine HEAD vs WP6 at defaults.

PRECONDITION
  verify-drift-hunt drives #drift-layer, i.e. the corridor FUSION, not the drift
  source. Every run below was taken with the fusion in the state named.

  Sync check at HEAD 1bb7d3c (WP7 land):
      git checkout -- prototypes/drift/drift-artifact.html
      node prototypes/corridor/tools/build-drift-layer.mjs
      git diff --stat -- prototypes/corridor/drift-layer.js drift-layer.css
    -> EMPTY.  The committed fusion IS in sync with the committed source at HEAD,
       so the pristine numbers below are the tree's own, not a stale layer's.

  WP6 runs were taken with the fusion rebuilt from the WP6 drift source
  (12/12 anchor patches, all asserted unique).

FIELD IS RANDOMISED
  drift-artifact.html shuffle() uses Math.random(); the layout, and therefore
  which checks are reachable, changes run to run.


=== PRISTINE HEAD 1bb7d3c (committed fusion) ===

run 1 — 4 fails
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged, hit-testable DOM interaction
        stage1=false/false, stage2=false/false; centre "過酷" -> "null" -> "null"
   FAIL a kana-only semantic word grows a meaningful same-level fallback constellation
        hitSelf=false, hit="現実は厳しい"; centre="厳しい"->"厳しい", DOM sats=12
   FAIL hub release cannot hijack a gesture or leave themed ghost satellites
        terminal 193px; deliberate release ctr="増加"->"増加", depth=""

run 2 — 4 fails
   FAIL a flick judgment sticks — tray ""; word back after 5s = true
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged  (stage1=false/false, stage2=true/false)
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

run 3 — 4 fails
   FAIL a flick judgment sticks — tray ""; word back after 5s = true
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged  (stage2=true/false)
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

run 4 — 6 fails
   FAIL a flick judgment sticks
   FAIL a release on a hub sun releases the constellation instead of diving
        hub "大" at (284,266) · held "増加" -> ctr 増加, depth ""
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged
   FAIL a kana-only semantic word grows a meaningful same-level fallback constellation
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

PRISTINE ENVELOPE ON THIS MACHINE: 4 · 4 · 4 · 6
STABLE CORE (4 of 4 runs): held-finger "no open water" · semantic-member-staged ·
                           hub-release


=== WP6, fusion rebuilt, DEFAULTS SELECTED (stage3 + staged) ===

run 1 — 4 fails
   FAIL a flick judgment sticks
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged  (stage2=true/false)
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)
   ok   no page errors across the regression battery

run 2 — 5 fails
   FAIL a release on a hub sun releases the constellation instead of diving
        hub "一" at (44,446) · held "増加" -> ctr 増加, depth ""
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged
   FAIL a kana-only semantic word grows a meaningful same-level fallback constellation
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

run 3 — 5 fails
   FAIL a flick judgment sticks
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged  (stage2=true/false)
   FAIL a kana-only semantic word grows a meaningful same-level fallback constellation
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

run 4 (final, after the last rebuild) — 4 fails
   FAIL a flick judgment sticks
   FAIL a held finger keeps a constellation alive past the 10s fade — no open water
   FAIL a corpus-backed semantic member is staged
   FAIL hub release cannot hijack a gesture  (terminal 193px, same signature)

WP6 ENVELOPE ON THIS MACHINE: 4 · 5 · 5 · 4


=== READING ===

Same failing set. Same signatures. Overlapping ranges (pristine 4-6, WP6 4-5).
No failure KIND appears under WP6 that does not appear under pristine.

The revised envelope of 2-3 recorded in wp7/verifier does NOT hold on this
machine at HEAD, BEFORE this change. That is reported, not argued away, and it
is not WP6's regression: the pristine runs above were taken on the committed
tree with the committed fusion proven in sync.

The stronger statement about the defaults is not here at all — it is
probes/probe-default-parity.mjs, which holds the field FIXED with a seeded
Math.random, forces both trees to walk the same words, and compares the full
semantic trace after every gesture: 3 seeds x 20 traced states, 0 differing.
The hunt re-rolls its field; the differential does not.
