# CN SWEEP findings ledger — 2026-08-28

Branch `review/cn-sweep-20260828`, base `claude/live-tweaks-20260827` @ merge-base `9926057b`.
One row per finding; each row lands in the same commit as its fix. FLAG rows have commit `—`.

| id | class | sev | area | location | finding | action | verification | commit |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-000 | FIX | P3 | sweep | gate-baseline.txt | baseline gate recorded before any change | ran §6 gate, saved output | lint/format/typecheck/test/node --check all exit 0; battery: per-gate results recorded | 8b388d90 |
| F-001 | FLAG | P2 | env | battery (corridor, corridor-a11y, writing-room, corridor-ai, e2e, native-readings, drift-fast) | Playwright chromium_headless_shell missing in this environment; browser gates could not run at baseline | attempting `npx playwright install chromium` (cache only, no repo change); whatever stays unrunnable is reported as SKIPPED, never claimed green | `browserType.launch: Executable doesn't exist` in /tmp/cn-sweep-battery-baseline/*.log | — |
| F-002 | FLAG | P2 | env | corpus-pytest | 18 collection errors at baseline — python deps missing in env | none (environment; corpus venv not part of this sweep) | `1 skipped, 18 errors in 0.26s` in corpus-pytest.log | — |
| F-003 | FIX | P1 | corridor-tools | docs/build-evidence/reading-r5-learning-loop-r5-20260815/residual-storage-callers.json | storage-integrity gate red at baseline: lineAfterPatch values stale after 19a132db (caller 13627→13978, bypasses 4881→4988, 4947→5054) | updated the three stale line numbers; surfaces/operations unchanged | `node prototypes/corridor/tools/verify-corridor-storage-integrity.mjs` exit 0, status PASS | fcb1ba53 |
| F-004 | FIX | P1 | bunki-desktop | prototypes/bunki-desktop/main.cjs:113 | suffix Range (`bytes=-N`) with N > file size computed negative `start` → `createReadStream` threw ERR_OUT_OF_RANGE → request 500s (README advertises byte-range support for the recorded voices) | clamp `start` to 0 per RFC 7233 §2.1 (entire representation when suffix exceeds length) | `node --check` ok; extracted `sendRange` harness: `bytes=-500` on 5-byte file → 206 `bytes 0-4/5` body "hello" (was ERR_OUT_OF_RANGE); `bytes=-3`→`2-4/5`; `bytes=-0`, `bytes=99-100`, `bytes=abc-def` still ignored | f9c4e138 |
| F-005 | FIX | P2 | bunki-desktop | prototypes/bunki-desktop/package.json:4 | description says shell wraps "the bunki-sites-v11 web app" — stale; main.cjs:3-14 and README.md:3-5 both state it serves the Corridor (掌 TENOHIRA line) | corrected description metadata to match README and main.cjs header | `node -e "JSON.parse(require('fs').readFileSync(...))"` parses; text now matches README.md:3-5 | 2c3ee40a |
| F-006 | FLAG | P2 | bunki-desktop | prototypes/bunki-desktop/main.cjs:59,69; package.json build.files | hardcoded absolute paths `/Users/dhyana/Bunki-app/...` (×2); packaged build ships only `main.cjs`, so the "bundled snapshot of origin/main — site/corridor" (main.cjs:12, README.md:11) is never inside the app — a packaged Bunki.app off the operator's machine finds no corridor | propose env-first refactor: keep BUNKI_SRC_DIR, replace hardcoded paths with `os.homedir()`-relative candidates, and either bundle `site/` or drop the packaged-snapshot claim; operator decision (packaging intent) | `grep -n "Users/dhyana" prototypes/bunki-desktop/main.cjs` → lines 59, 69; package.json `files: ["main.cjs"]` | — |
| F-007 | FLAG | P3 | bunki-desktop | prototypes/bunki-desktop/main.cjs:153-155 | symlink escape not blocked: a symlink inside the corridor tree pointing outside `src` would be served (guard only normalizes `..`); no symlinks exist in the repo today and the server binds 127.0.0.1 only | none (ledger only); if ever needed, realpath-check `file` against `src` | `find prototypes/corridor prototypes/bunki-desktop -type l` (excl. node_modules) → empty | — |
| F-008 | FLAG | P3 | bunki-desktop | prototypes/bunki-desktop/main.cjs:153-155 | CLEAN verdict: path-traversal guard neutralizes raw and encoded `..` (WHATWG URL parser resolves dot segments incl. `%2e`; normalize+startsWith fallback catches the rest) | none | probe harness on the exact guard logic: `/../etc/passwd`, `/%2e%2e/%2e%2e/etc/passwd`, `/..%2f..%2fetc%2fpasswd`, `/sub/../../secret` all resolve inside src or fall back to index.html | — |
| F-009 | FLAG | P3 | bunki-desktop | prototypes/bunki-desktop/main.cjs:30-52,131-151 | CLEAN verdict: no SSE client leak on repeated reloads — `req.on('close')` deletes the client and clears its heartbeat; minor: `broadcastReload`'s catch (line 35) deletes a client without clearing its 25 s interval, which then lingers until socket close | none (ledger only; one-line `res.end()` in the catch would close the gap, not worth a behavior commit) | code read of lines 30-52 & 131-151; EventSource close on `location.reload()` fires req `close` | — |
| F-010 | FLAG | P3 | bunki-desktop | prototypes/bunki-desktop/main.cjs:40-52; tools/add-launch-shim.cjs:33 | CLEAN verdicts: `fs.watch recursive` is macOS/Windows-only but this shell is mac-only (package.json `build.mac`) and the call is try/catch best-effort; shim quoting `exec "${0%/*}/Bunki-bin" "$@"` is correct | none | code read; package.json build target is `mac` only | — |
| F-011 | FIX | P1 | corridor | prototypes/corridor/corridor.js:2510 (back) | `back()` had no `S.view === 'list'` branch: on a per-list page with empty stack the chrome 戻る button is enabled (`atHome` false, corridor.js:13836-13838) yet `back()` fell through every branch and did nothing; device Back funnels into the same path | added a `list → tray` branch mirroring the in-page ← リスト一覧へ chip (corridor.js:4926-4931), the only entry point being the tray door at corridor.js:4719-4725 | `node --check` on all 5 corridor JS files ok; traced: list view entered only at 4722 with empty stack; pre-fix fall-through verified at 2510-2560 | 31ca9eb4 |
| F-012 | FIX | P1 | corridor | prototypes/corridor/corridor.js:12668 (sheet-search door) | focus steal: sheet opened from a search result row records `S.dialogInvoker = {kind:'nav-row'}`; the sheet-search door ran `dismissSheet()` → `restoreDialogInvoker()` rAF, which fired a frame after `openSearchPage()`'s synchronous input focus and moved focus to the restored result row — iOS drops the keyboard, defeating the door's one-tap-to-type purpose | null `S.dialogInvoker` before `dismissSheet()` so the restore no-ops; scoped to this door only (the other three doors never carry a live invoker) | `node --check` ok; traced invoker set (2408-2409/2342), rAF restore (2351-2366), rows re-rendered from `S.navQ` so the stale row exists at fire time (13637-13641) | f0f7652e |
| F-013 | FIX | P1 | corridor | prototypes/corridor/corridor.js:7736 (schedulePreview) | FSRS preview table's 日時/due cell used `next.due.toISOString().slice(0,16)` — UTC printed as wall-clock; a JST learner's 08:00 review rendered as "2026-08-27 23:00" | build the same `YYYY-MM-DD HH:MM` shape from local components via `dayKey(next.due)` + local hours/minutes | TZ=Asia/Tokyo repro: old → `2026-08-27 23:00`, new → `2026-08-28 08:00`; TZ=UTC: old === new; `node --check` ×5 ok | a2e70486 |
| F-014 | FIX | P2 | corridor | prototypes/corridor/corridor.js:4943 (renderListPage h1) | list-page title `` `${open.name} — ${items.length} 件` `` was raw JA-only — the only mode-blind user-visible string in the 2026-08-27 commit; EN mode read "2026年8月 — 3 件" | route through `tx()` with EN pluralization, matching the sibling view-title pattern at corridor.js:4513 | `node --check` ×5 ok; pattern matches 4513/8115 (`item${... === 1 ? '' : 's'}`) | d7eca2c6 |
| F-015 | FIX | P2 | corridor | prototypes/corridor/drift-layer.js:3075 (hide) | a long-press armed within 430 ms of the layer sleeping still fired: `pointerup` early-returns when `!DRIFT_ON` (drift-layer.js:2375) so nothing cleared `lpTimer`, and the callback (2275-2280) has no DRIFT_ON check — `constellationLock` ran on the hidden layer, leaving an invisible lock active on return | `clearTimeout(lpTimer)` in `hide()`; timer is module-scoped (drift-layer.js:2171) and cleared the same way on move/cancel/up paths | `node --check` ×5 ok; scope verified: `lpTimer` declared at 2171, cleared at 2221/2227/2272/2372/2381 — hide was the only exit that did not | 48653992 |
| F-016 | FIX | P3 | corridor | prototypes/corridor/corridor-ink.js:748,939 | dead `done` variable declared in both `startGPU` and `startGL2` factory state lines — never read or written anywhere in either body (the finish path uses `writing`/`looping`; `'done'` hits are the phase string) | removed the dead binding from both declarations | `awk` range scan of both function bodies: `done` appears only in the declarations; `node --check` ok | b207fd6a |
| F-017 | FIX | P3 | corridor | prototypes/corridor/corridor.js:12678,13721,13758,13916 | the magnifier SVG string literal was duplicated byte-identical in all four search doors (sheet bar, ginga ghost, nav bar, top chrome) | hoisted to `const SEARCH_SVG` beside `GINGA_SYMBOL_SVG` (corridor.js:13523); all four doors reference it | pre-fix `grep -o … \| sort -u \| wc -l` → 1 (byte-identical); post-fix literal count = 1 (declaration only); `node --check` + `prettier --check` ok | fed75ff9 |
| F-018 | FIX | P3 | corridor | corridor.css (drift-door, variants-bar, stroke-close, stroke-title-glyph, stroke-number-choice, stroke-palette-*) + corridor.js:538,12434 (strokePaletteFocus) | dead code that looks alive: 8 CSS selectors with no DOM producer in any shipped file, plus the vestigial stroke-palette focus-restore branch — `S.strokePaletteFocus` was only ever null (init corridor.js:538, read-and-nulled at 12435), so the `.stroke-palette-choice` query could never match | removed the dead CSS rules (grouped selectors edited surgically) and the dead JS branch + state key | `grep -c` each token across corridor.js/drift-layer.js/corridor-ink.js/index.html/sw.js → 0 (choice/paletteFocus only in the removed branch); post-fix repo-wide grep → 0 hits; `node --check` ok | 7cf5b6c0 |
| F-019 | FLAG | P2 | corridor | prototypes/corridor/corridor.js:13949 | ghost capture panel: `toggleTaken(...)`'s false return (persist failure) is ignored and `S.captureOpen = true` still opens the capture panel for a word that was not taken; pickers bail (7566, 7602) so it self-heals visually | propose `if (toggleTaken(...)) { S.captureOpen = true; render(); } else render();` — one line, but touches a POL-adjacent flow; left for reviewer | code read: 13949 ignores the return; commitStorePatch never leaves mutated roots (1183-1188) so the panel is a ghost of an action that did not happen | — |
| F-020 | FLAG | P2 | corridor | prototypes/corridor/corridor.js (S.listMenuFor 7487/7626/12693; S.studyOpen 9302) | ephemeral fold keys are never cleared by `go()` (2406) or `dismissSheet()` (2562): dismissing and revisiting the same entry later shows the drawer/fold unexpectedly open; wrong-word leak is impossible (values are keyed `${t}\|${id}` / srsKey and compared against the current sheet's node) | two defensible answers ("fold stays as you left it" vs reset) — if reset is wanted, `S.listMenuFor = null; S.studyOpen = null;` in dismissSheet; operator decision | keying verified: listMenuFor read only at 7604 with the sheet's own node (10739, 11010, 12507, 12535, 14008); studyOpen read at 9284 same-node | — |
| F-021 | FLAG | P3 | corridor | prototypes/corridor/corridor.js:4920-4924 (renderListPage fallback) | deleted-list fallback mutates `S.view = 'tray'` mid-render after the chrome was painted for 'list' (body dataset + crumb computed pre-fallback); self-corrects on the next render | could add `S.listOpen = null` and/or bail earlier; cosmetic | code read: fallback at 4920-4924 vs chrome paint at ~13800 | — |
| F-022 | FLAG | P2 | corridor | prototypes/corridor/corridor.js:9345-9354, 12560 (encounterTrail) | dojo revocation rows `[t, 'dojo', key, 0]` (undo, written at 8651) are still counted as 道場 practice by the study fold (kinds.dojo += 1 with no r[3] check) and by encounterTrail — an undone drill inflates the evidence display | judgment call: skip `r[3] === 0` dojo rows in both counters, or treat the attempt as a real encounter; operator decision | study fold loop at 9347-9350 has no revocation check; revocation writer at 8651 confirmed `[Date.now(), 'dojo', key, 0]` | — |
| F-023 | FLAG | P3 | corridor | prototypes/corridor/corridor.js:7968-7970 (srsForecast) | forecast bucket edges computed as `startOfDay(now) + n·86400000` — across a DST transition a bucket boundary lands one hour off (srsWhen itself is safe: Math.round absorbs 23/25-h midnights) | edge-only, twice a year, ±1h miscount; fix would use calendar-day arithmetic | code read vs srsWhen 7944-7959 | — |
| F-024 | FLAG | P3 | corridor | prototypes/corridor/corridor.js:4908 | the list .md export header (`# name — 分岐 Bunki` / `N 件 · dayKey()`) is JA-only regardless of UI mode — the only mode-blind user-facing artifact from 19a132db; defensible for a study-note export | operator decision (export artifact voice) | code read at 4908; sibling h1 fixed in F-014 | — |
| F-025 | FLAG | P2 | corridor | prototypes/corridor/corridor-ink.js:1077-1100 (GL2 frame) | GL2 frame loop re-arms rAF before doing work and has no try/catch, unlike the GPU path (811-832: catch → dead → ondead fallback); a throw in uploadSprite/splat/step/render (e.g. `new Path2D` on malformed outline data) would spam uncaught errors every frame and never engage the corridor's SVG fallback | asymmetric hardening; trigger requires bad corpus data (both outline sources are gate-validated), so left as FLAG with the GPU path as the template | code read both frame loops; GPU try/catch at 821-831 vs GL2 bare body at 1089-1100 | — |
| F-026 | FLAG | P2 | corridor | prototypes/corridor/corridor.css:603 | `.nav-bar { position: relative; }` — a bare uncommented 3-line override (commit 67e40929) of the fixed-position rule at 448; the leftover `top/left/transform/z-index` from 448 now act as relative offsets, so the bar's geometry is an accident of two rules | deliberate commit, but if the intent was "no longer fixed" the 448 rule should have been edited; cannot confirm visual breakage without rendering — operator/reviewer call | `git log -L` attributes 603 to 67e40929; single .nav-bar producer at corridor.js:13706 | — |
| F-027 | FLAG | P3 | corridor | prototypes/corridor/sw.js:17-30, 56, 78-90 | precache boundary: the 12-file SHELL is all that installs offline; fonts/audio/data shards are only cached after one online fetch (network-first + opportunistic put), so a brand-new install opened offline has no fonts/data/audio — the "opens in a tunnel" comment holds only after one online run | documented two-rule design (sw.js:4-11); widening the precache is a size/policy decision for the operator | SHELL list vs pages-app.yml copy list compared line by line — all 12 exist and ship | — |
| F-028 | FLAG | P3 | corridor | corridor.js:4677-4705 vs 7681-7710; doors 12655/13686/13724/13881 | remaining duplication deliberately NOT deduped: the two list-maker forms diverge behaviorally (tray rejects duplicate names inline 4688-4694; sheet reuses and appends 7696-7701); the four search-door handlers diverge by design (dismissSheet+open / bare / bare / stack=[]+open) — only their SVG was byte-identical (hoisted, F-017) | a shared door-button helper is possible but not clearly simpler given the divergent handlers; PROP territory, not spent | side-by-side read of both makers and all four doors | — |
| F-029 | FLAG | P3 | corridor | corridor.js:1442-1477 ↔ dictionary-worker.js:489-507 | dictionary-worker has no init/search timeout and no `messageerror` listener: a wedged worker would leave `dictionaryState='loading'` forever and the concurrency-1 gate (6743) would block later searches; reachable only via browser-level pathology, not app code | observation only; worker-crash path IS covered (error listener rejects all pending, 1475-1477) | contract traced both directions: 4 request types, generation echo, one response per request, unknown types reject explicitly (worker:502) | — |
| F-030 | FLAG | P3 | corridor | corridor.js ★ clean verdicts | CLEAN: commitStorePatch boundary (all 30 call sites listed, no pre-commit mutation of persisted roots; ignored returns at 4770/4997/5647-5651 benign or deliberate POL-13); render() re-entrancy + keepScroll/returnScroll pairs; trayLine t/id matching (4854) with remaining `===` comparisons all same-reference by construction (7591, 8253); obslog `[ts,kind,key,...]` shape across every writer (1279/3625/5096/5357/7874/8424/8651/9223) + load validator (702-736); search-door cleanup on all 4 doors; empty monthly bucket + startReview suspended/unstarted rows (7910-7917, 7992-8003) | none needed | full-call-site audit tables in session logs; spot-verified 702, 8651, 9345-9350, 12560 | — |
| F-031 | FLAG | P3 | corridor | sw.js / index.html / manifest.webmanifest ★ clean verdicts | CLEAN: all 12 SHELL entries exist in repo and are copied by pages-app.yml; VERSION policy documented inline (sw.js:8-11) and in sync (kairo-v2 ↔ dict-v2 schema-3 pin pages-app.yml:122); https-only registration guard comment (index.html:48-49) matches the code (51); manifest icons/start_url/scope all exist and ship; every index.html reference resolves | none needed | entry-by-entry table: sw.js:17-30 vs pages-app.yml:53-70; index.html refs at 18/20/34/39/40/46/47/52 | — |
| F-032 | FLAG | P3 | corridor | corridor.css ★ clean verdicts | CLEAN: zero byte-identical duplicate rules (42 repeat selectors all complementary or documented restyles); the 2026-08-27 block (6262-6441) uses theme vars only — no hardcoded colors, dark-world contrast inherits; dynamic classes `.g-*`/`.t-*` have live template producers (7334, 8111, 8500, 9177) | none needed (dead selectors removed under F-018; .nav-bar override flagged F-026) | per-selector producer greps; block read 6262-6441 | — |
| F-033 | FLAG | P3 | corridor | dictionary-worker.js / corridor-ink.js / drift-layer.js clean verdicts | CLEAN: worker message contract matches both directions with generation echo and explicit unknown-type rejection; corridor-ink is module-only with zero window refs/listeners/timers and wired GPU→GL2→SVG fallbacks; drift-layer's window writes all deliberate and matched to corridor.js readers, listeners guarded by DRIFT_ON, rAF self-terminates on hide | none needed (lpTimer edge fixed F-015; GL2 asymmetry flagged F-025) | contract tables in session logs; spot-verified drift-layer.js:3068-3084, corridor-ink.js:748/939/1077-1100 | — |
