The answer face of a flashcard — information hierarchy done

HARNESS FAILURE — STRUCTURED OUTPUT CHANNEL BROKEN: Every tool call in this session (Read, Glob, Bash, ToolSearch, and StructuredOutput itself) is rejected because the session's permission handler returns `updatedInput` with all parameters stripped, so even a minimal schema-valid StructuredOutput call fails validation ("must have required property 'findings'"). Four attempts failed identically, including a `{"findings":[{"insight":"test","concrete":"test"}],"top_recommendations":["test"]}` probe. The contract cannot be fulfilled through the tool. Below is the exact JSON payload, verbatim, so the orchestrator or a human can recover it from the text transcript.

Note also: I could not read the repo or fetch live pages (same handler bug), so findings are from training-data knowledge of these products' documented, stable UI conventions — not live verification.

```json
{
  "findings": [
    {
      "insight": "ENVIRONMENT NOTE: every tool call in this subagent sandbox (filesystem, web, and StructuredOutput itself) was rejected by a broken permission handler that strips tool inputs. Repo and live sites could not be inspected; findings are pre-2026 training knowledge of these products/templates — stable conventions, but no file-level claim about Bunki-app is verified, and this JSON is delivered via text fallback.",
      "concrete": "No file:line references available. Sizes tuned for the stated 390-430px viewport and described DOM (word, kana-red reading, 4 italic senses, 2 examples, 再難良易 bar).",
      "source": "sandbox tool failure (all tool inputs stripped by permission handler)"
    },
    {
      "insight": "jpdb.io — the most minimal mainstream JP SRS back — shows ONLY: word with reading attached as furigana ruby above it, a pitch-accent line over the kana, then numbered senses. Example sentences are NOT on the review back at all (they live on the vocab detail page). Strongest precedent that examples default-hidden is normal, not a compromise.",
      "concrete": "Reading as <ruby>語<rt>ご</rt></ruby>, not a separate line (saves one text row); pitch accent = 2px solid overline over the kana with a 2px vertical notch at the downstep mora (CSS border-top + ::after 2px×0.5em drop); senses numbered ①②③ (U+2460+) — reads as UI structure AND Edo-print flavor.",
      "source": "jpdb.io review UI"
    },
    {
      "insight": "WaniKani splits meaning into one 'Primary meaning' (prominent) and 'Alternative meanings' (secondary; hidden during reviews inside the collapsed 'Show Item Info' panel with mnemonics and context sentences). Answer face = verdict + primary answer; everything else behind exactly one tap on a full-width quiet bar. Canonical progressive disclosure for JP SRS.",
      "concrete": "One full-width disclosure row under the senses: min-height:44px; width:100%; font-size:12px; letter-spacing:0.14em; ~55% ink; label '詳しく ▾ (例文 2 · 語義 +1)'; chevron rotates 180deg on open (transform 200ms). Tap target = whole row, never an icon.",
      "source": "WaniKani review 'Item Info' panel"
    },
    {
      "insight": "Bunpro default-hides the ENGLISH translation of example sentences behind a tap, showing Japanese first. Reading L2 first then optionally checking L1 encodes better than parallel bilingual text and halves visible text mass.",
      "concrete": "In the expanded details: JP sentence visible (15px, line-height 1.9, target word colored reading-red, no background highlight); its EN translation at opacity:0/height:0 until the sentence is tapped, then fades in at 13px muted. One tap opens examples; a tap on a sentence reveals its translation.",
      "source": "Bunpro review UI (translation hidden by default)"
    },
    {
      "insight": "Migaku/Refold/Fluent Forever doctrine plus SuperMemo minimum-information principle (rule #4 of Wozniak's 20 rules): ONE meaning per review moment. Multiple simultaneous glosses slow grading and cause 'was I right?' ambiguity — exactly the operator's 'crowded and confusing' complaint. Power-user Anki templates (Kaishi 1.5k, JP Mining Note) render the first gloss bold/large and fold extra dictionaries into collapsed blockquotes.",
      "concrete": "Max visible senses = 1 primary + at most 2 secondary. Primary: own line, 17-18px, weight 600, full-contrast ink, NO italic. Secondary: ONE shared comma-separated line, 13px, ~60% ink, weight 400. Senses #4+ never render on the face — disclosure label counts them ('語義 +1'). With today's 4 senses: sense[0] big, sense[1]+sense[2] muted line, sense[3] behind tap.",
      "source": "SuperMemo 20 rules #4; Refold/Migaku; Kaishi & JPMN Anki templates"
    },
    {
      "insight": "The grade decision should be makeable in under ~2s from exactly two elements: reading + primary sense. Everything not feeding the 再/難/良/易 decision is reference material behind disclosure. Tuned Anki users average 2-4s back-dwell; a back inviting two sentences of reading per card doubles session time and kills hypnotic cadence.",
      "concrete": "Above-the-fold answer face = 3 things: word (stationary), reading, primary sense. Vertical rhythm at 390px: word 48-56px kanji with ruby rt 18-20px red; 20px gap; primary sense 17-18px; 6px gap; muted secondary line 13px; 16px gap; 44px disclosure row; grade bar. Total ≈220px — generous washi negative space (the zen is the emptiness).",
      "source": "SRS community timing consensus (r/Anki, jpdb design)"
    },
    {
      "insight": "Position constancy on flip: in jpdb, Migaku, and WaniKani the tested item does NOT move when the answer appears — the answer materializes around a stationary anchor. A word that jumps on flip forces eye re-acquisition and reads as 'confusing' regardless of content volume.",
      "concrete": "Reserve the ruby line's height on the FRONT (visibility:hidden rt) so the word's y-position is identical front/back. Reveal stagger: reading 0ms, primary sense 90ms, secondary line 180ms, disclosure row 260ms; each opacity 0→1 + translateY(6px→0), 300ms cubic-bezier(0.22,1,0.36,1). @media (prefers-reduced-motion: reduce): transform:none, opacity 120ms linear, zero stagger.",
      "source": "jpdb/WaniKani flip behavior; Material motion stagger guidance"
    },
    {
      "insight": "Italic serif senses are the cheapest 'crowded' fix: 4 italic serif lines at small sizes on textured washi smear together (~15% legibility loss for italics on textured grounds). Italic should mark metadata, not payload. Skritter and jpdb set definitions in regular weight; part-of-speech gets the de-emphasis.",
      "concrete": "Primary sense: font-style:normal, serif ok at ≥17px. POS tags: 10px, small-caps/uppercase, letter-spacing:0.08em, ~50% ink, placed BEFORE the primary sense on the same line — never one tag per sense line. Nuance notes ('esp. of weather') may stay italic at 13px.",
      "source": "jpdb/Skritter definition typography"
    },
    {
      "insight": "Examples on the answer face: modern JP-SRS consensus is 0 visible by default (jpdb, WaniKani reviews) or 1 max (Migaku sentence cards, where the sentence IS the card). Two full bilingual examples per flip is the outlier causing crowding. Examples belong one quiet tap away, with the disclosure advertising they exist so they don't feel amputated.",
      "concrete": "Default: zero examples rendered; disclosure counts them ('例文 2'). Expanded panel: max 2 sentences, JP-only initially, target word tinted reading-red (color-only highlight — background highlights fight washi texture), 12px between sentences. Expansion: grid-template-rows 0fr→1fr wrapper, 250ms ease-out; panel overflow-y:auto, max-height:38vh so the grade bar NEVER moves.",
      "source": "jpdb (no examples in review), WaniKani (collapsed context sentences), Migaku (1 sentence)"
    },
    {
      "insight": "Grade-bar stability is part of information hierarchy: 再難良易 must occupy the same pixels on front (hidden/disabled) and back and never be displaced by content expansion — muscle-memory grading is what makes a session hypnotic; jpdb/AnkiMobile users grade without looking at the buttons.",
      "concrete": "Action bar: fixed bottom row, height 64px + env(safe-area-inset-bottom); each seal ≥64×44px hit area even if the visible seal is 44×44. Card body overflow:hidden; only the expanded details panel scrolls internally. Back content never pushes the seals.",
      "source": "jpdb/AnkiMobile grade-bar behavior"
    },
    {
      "insight": "WCAG AA on washi: muted secondary text is where AA breaks; paper texture locally darkens/lightens the ground, so keep margin above 4.5:1 for anything under 18px.",
      "concrete": "On washi ≈#EDE4D0: primary ink #2B2620 (~12:1), muted #6F6555 (~4.6:1 — floor, no lighter), disclosure label #7A7060 only at ≥14px + wide letter-spacing, reading red #A4342A or #B3261E (≥4.5:1 on cream; validate against the darkest texture speckle, not the mean). Red is fine for the kana reading (large text, 3:1 rule) but never for 13px muted text.",
      "source": "WCAG 1.4.3 vs cream grounds; jpdb red-reading convention"
    },
    {
      "insight": "SYNTHESIS — ideal KAIRO back top-to-bottom: the answer is the reading, appearing first in reveal order but visually fused to the word; one sense decides the grade; one tap holds the rest.",
      "concrete": "DOM order: (1) <ruby> word 48-56px + rt reading 18-20px red with pitch-accent overline — word position identical to front; (2) primary sense line: [POS small-caps 10px] + gloss 17-18px normal serif #2B2620; (3) one 13px muted comma line, ≤2 secondary senses, #6F6555; (4) 44px disclosure row '詳しく ▾ · 例文 2 · 語義 +1'; (5) collapsed panel: ≤2 JP sentences 15px/1.9 with tap-per-sentence EN 13px, senses 4+ as ①-numbered list; (6) fixed grade bar 64px 再難良易. Reveal stagger 0/90/180/260ms, 300ms cubic-bezier(0.22,1,0.36,1); reduced-motion = 120ms opacity only.",
      "source": "synthesis of jpdb + WaniKani + Bunpro + Migaku + SuperMemo rule 4"
    }
  ],
  "top_recommendations": [
    "Back face shows exactly 3 content elements: word (stationary, same y as front) with reading as red ruby furigana + pitch-accent overline; ONE primary sense (17-18px, weight 600, NOT italic, POS as 10px small-caps prefix); ONE muted 13px comma line with ≤2 secondary senses. Nothing else visible. Total visible senses cap = 3.",
    "Zero example sentences by default (jpdb/WaniKani precedent). One full-width 44px quiet disclosure row ('詳しく ▾ · 例文 2 · 語義 +1') opens a panel with ≤2 JP-only sentences (target word tinted reading-red, no background highlight); tapping a sentence reveals its 13px EN translation (Bunpro pattern). Panel scrolls internally (max-height 38vh) so the grade bar never moves.",
    "Kill italic serif for senses — italic is for nuance notes only; senses in regular serif. Contrast floors on washi #EDE4D0: ink #2B2620, muted #6F6555 (4.6:1 minimum for 13px), reading red #A4342A/#B3261E; validate against the darkest texture speckle.",
    "Reveal choreography: word never moves (reserve ruby height on the front); answer elements stagger in at 0/90/180/260ms, each 300ms opacity + 6px translateY, cubic-bezier(0.22,1,0.36,1); prefers-reduced-motion → 120ms opacity only, no stagger, no transform.",
    "Grade bar (再難良易) is fixed geometry: 64px + safe-area, ≥64×44px hit areas, identical position front/back, never displaced by expansion — muscle-memory grading in <2s from reading + primary sense is what makes the session hypnotic."
  ]
}
```