Audio on flashcards, minimal and inevitable. Research how W

The environment's permission handler is broken for every tool in this session — all calls (Bash, Read, Glob, ToolSearch, and StructuredOutput itself, four attempts, ASCII-only included) fail with the harness stripping tool inputs before validation ("updatedInput failed schema validation" / "must have required property"). The StructuredOutput call cannot be landed. Below is the exact payload, schema-conformant JSON, for the synthesis agent to consume as text.

```json
{
  "findings": [
    {
      "insight": "SRS apps anchor audio to the content it voices, never a toolbar: Anki renders its replay button inline at the [sound:] tag position; jpdb puts a small muted speaker glyph beside the headword/reading; WaniKani's speaker sits next to the reading in the item-info panel. For KAIRO: glue the glyph to the kana reading line on the card back.",
      "concrete": "HTML: <div class=\"reading-row\"><span class=\"reading\">かいろ</span><button class=\"say\" aria-label=\"読み上げ — play pronunciation\">…</button></div>. CSS: .reading-row{display:flex;align-items:center;justify-content:center;gap:10px} .say{width:44px;height:44px;margin:-12px 0;padding:0;background:none;border:0;color:#847a68;touch-action:manipulation;-webkit-tap-highlight-color:transparent} — glyph 20px inside the 44px target; negative margin keeps line height from growing.",
      "source": "Anki AV-tag rendering / jpdb card layout / WaniKani item-info panel (product knowledge; live fetch unavailable — env tool handler broken)"
    },
    {
      "insight": "Icon form: skip the Western speaker-box. Use the kanji 音 (U+97F3, 'sound') inside a hairline ring — a fifth, smaller seal rhyming with the 再難良易 grade seals; the ring disambiguates control from content (bare 音 could read as card text).",
      "concrete": "<svg viewBox=\"0 0 24 24\" width=\"20\" height=\"20\" aria-hidden=\"true\"><circle cx=\"12\" cy=\"12\" r=\"10.5\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.25\"/><text x=\"12\" y=\"12.5\" text-anchor=\"middle\" dominant-baseline=\"central\" font-size=\"11\" fill=\"currentColor\" font-family=\"'Hiragino Mincho ProN','Yu Mincho','Noto Serif JP',serif\">音</text></svg>. CSS-only fallback: 26px circle — border:1px solid currentColor;border-radius:50%;font:11px/24px 'Hiragino Mincho ProN',serif; text content 音 — wrapped in the 44px padded button.",
      "source": "Design synthesis; matches existing grade-seal motif"
    },
    {
      "insight": "Non-kanji alternative (futuristic-minimal): arcs-only sound waves — Feather 'volume-2' with the speaker triangle deleted. Zen apps (Calm/Medito/Oak players) converge on 1.25–1.5px hairline strokes at 20–24px; active = color shift, never fill or motion.",
      "concrete": "<svg viewBox=\"0 0 24 24\" width=\"20\" height=\"20\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\"><path d=\"M15.54 8.46a5 5 0 0 1 0 7.07\"/><path d=\"M19.07 4.93a10 10 0 0 1 0 14.14\"/></svg> — optically center by shifting x −3px (paths are right-biased).",
      "source": "Feather icons volume-2 paths (MIT); zen-app player styling (product knowledge)"
    },
    {
      "insight": "Autoplay defaults across apps track audio QUALITY: Anki ON (curated recordings; per-deck 'Don't play audio automatically' opt-out); WaniKani ON in lessons, opt-in in reviews; jpdb per-deck front/back toggles. KAIRO uses interim device TTS → default OFF, tap-to-hear primary. Crucially: the reveal tap IS a user gesture, so iOS Safari's gesture requirement is satisfied for autoplay-on-reveal — call speak() from the reveal tap handler, delayed only by setTimeout.",
      "concrete": "Setting key autoSpeakOnReveal:false (default). When true: setTimeout(()=>tts.speak(reading,el),350) inside the reveal handler — 350ms lets the flip transition land first (Anki likewise plays after render). Settings row copy: '音 — speak reading on reveal (device voice)'. No rate/pitch UI in v1.",
      "source": "Anki deck options / WaniKani Settings>App / jpdb deck settings (product knowledge)"
    },
    {
      "insight": "SRS-integrity rule every app obeys: audio only AFTER reveal — the reading IS the tested content (WaniKani plays vocab audio only after the answer is submitted). The affordance must be absent from the card front, not merely hidden or disabled, or a stray tap leaks the answer.",
      "concrete": "Render .say only in the back-side template; JS guard: if(!card.revealed) return; No front-side long-press easter egg.",
      "source": "WaniKani review flow (product knowledge)"
    },
    {
      "insight": "Speaking-in-progress without animation noise: a discrete state swap, not a spinner/equalizer. Idle muted ink → speaking vermillion + a static hairline ring materializes one step outside the seal; revert on end. Color/opacity only — zero transform, zero layout shift. One optional ultra-slow opacity 'breath' only when motion is allowed.",
      "concrete": ".say{color:#847a68;transition:color 200ms ease;position:relative} .say.is-speaking{color:#b2352f} .say.is-speaking::after{content:'';position:absolute;inset:8px;border:1px solid currentColor;border-radius:50%;opacity:.3} @media (prefers-reduced-motion:no-preference){.say.is-speaking svg{animation:breath 2400ms cubic-bezier(.37,0,.63,1) infinite}} @keyframes breath{0%,100%{opacity:1}50%{opacity:.6}} @media (prefers-reduced-motion:reduce){.say{transition:none}}",
      "source": "Calm/Medito player state conventions; prefers-reduced-motion spec"
    },
    {
      "insight": "WCAG 1.4.11 (non-text UI needs 3:1 vs adjacent bg): #6b6152 on washi #efe7d8 ≈ 4.8:1 but FAILS if dimmed to opacity .55 (composited ≈ 2.1:1). Express 'muted' via a passing color at full opacity, never transparency. Speaking #b2352f on washi ≈ 5.1:1 (pass).",
      "concrete": "Idle: color:#847a68 at opacity:1 (≈3.4:1 on #efe7d8, passes 3:1). Reserve opacity change for the transient speaking breath only (floor .6 of full-contrast #b2352f); the static ring stays full-stroke.",
      "source": "WCAG 2.1 SC 1.4.11 arithmetic on stated palette"
    },
    {
      "insight": "Retap semantics: tap while speaking = cancel + restart from the beginning (Anki's R/F5 replay behavior). No pause state — word utterances are under 2s; pause UI is pure clutter. Debounce accidental double-taps.",
      "concrete": "onclick: if(Date.now()-last<150)return; last=Date.now(); tts.speak(text,btn); // speak() begins with speechSynthesis.cancel(), safe when idle. aria-label stays static ('読み上げ — play pronunciation'); do NOT toggle aria-pressed (action, not toggle).",
      "source": "Anki replay-audio behavior (product knowledge)"
    },
    {
      "insight": "Web Speech engine contract for iOS/Android: getVoices() is empty until voiceschanged fires; prefer a localService ja-JP voice (iOS: Kyoko); cancel on page-hide to avoid iOS synthesis wedging; hide the button ENTIRELY when no ja voice exists (a dead speaker is worse than none); speak the KANA reading string, not the kanji headword — TTS misreads rare kanji, kana is deterministic.",
      "concrete": "const tts={voice:null,init(){const pick=()=>{const v=speechSynthesis.getVoices().filter(x=>x.lang.replace('_','-').startsWith('ja'));tts.voice=v.find(x=>x.localService)||v[0]||null;document.documentElement.classList.toggle('no-tts',!tts.voice)};pick();speechSynthesis.addEventListener('voiceschanged',pick)},speak(t,el){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(t);u.lang='ja-JP';if(tts.voice)u.voice=tts.voice;u.rate=.9;u.pitch=1;u.onstart=()=>el.classList.add('is-speaking');u.onend=u.onerror=()=>el.classList.remove('is-speaking');speechSynthesis.speak(u)}}; document.addEventListener('visibilitychange',()=>{if(document.hidden)speechSynthesis.cancel()}); CSS: .no-tts .say{display:none}",
      "source": "Web Speech API platform behavior, iOS Safari quirks (knowledge; live verification unavailable)"
    },
    {
      "insight": "Sentence audio: the card back is already 'crowded and confusing' per brief, so v1 ships word-level audio ONLY. If added later, follow jpdb/Migaku: per-sentence end-of-line micro-glyph in the SAME seal style — never a second button vocabulary.",
      "concrete": "v2 only: same 音 seal at 16px trailing each sentence: .say--s{width:16px;height:16px;color:#847a68;position:relative} .say--s::before{content:'';position:absolute;inset:-14px} (16 + 2×14 = 44px effective target, invisible inflation). Max one per sentence, always trailing.",
      "source": "jpdb sentence rows / Migaku sentence-audio controls; operator brief"
    },
    {
      "insight": "Discoverability without permanent labels: teach once, then go silent (zen-app coach-mark pattern). One whisper caption under the reading row for the first 3 reveals ever, then never again.",
      "concrete": "<div class=\"say-hint\">触れて音 · tap for audio</div> .say-hint{font-size:11px;letter-spacing:.08em;color:#847a68;margin-top:2px;text-align:center} JS: const n=+localStorage.getItem('kairo.audioHint')||0; if(n<3){show();localStorage.setItem('kairo.audioHint',n+1)}",
      "source": "Headspace/Calm one-time coach marks (product knowledge)"
    },
    {
      "insight": "The 'interim device voice' disclosure belongs in Settings, not on the card — jpdb similarly footnotes TTS quality in deck settings; cards stay clean. On-card marking would fight the zen brief.",
      "concrete": "Settings sub-caption under the autoplay toggle: 'Uses your device's Japanese voice. Recorded audio comes later.' 12px, color #847a68. Nothing on the card itself.",
      "source": "jpdb settings copy pattern (product knowledge)"
    }
  ],
  "top_recommendations": [
    "PLACEMENT+FORM: single audio affordance on the card BACK only — a 音-in-hairline-ring 'sound seal' (SVG: circle cx12 cy12 r10.5 stroke-width 1.25 + 11px serif 音; 20px glyph) on the kana reading's flex row (gap:10px) inside a 44×44 button with margin:-12px 0 so line height doesn't grow. Never on the front — the reading is the answer.",
    "BEHAVIOR: tap = speak the KANA string via Web Speech (ja-JP localService voice, rate 0.9, cancel-before-speak); retap restarts (150ms debounce, no pause state); autoSpeakOnReveal defaults OFF (interim TTS quality); when ON it fires via setTimeout 350ms inside the reveal tap handler — that tap satisfies iOS Safari's user-gesture requirement, so autoplay-on-reveal is safe.",
    "SPEAKING STATE, NO MOTION NOISE: discrete swap — idle #847a68 (≈3.4:1 on washi #efe7d8, passes WCAG 1.4.11) → speaking #b2352f (≈5.1:1) plus a static 1px ring at inset:8px opacity .3; transition color 200ms ease only; optional 2400ms cubic-bezier(.37,0,.63,1) opacity breath (1→.6) gated behind prefers-reduced-motion:no-preference; transition:none under reduce.",
    "ROBUSTNESS: hide the control entirely when no ja voice exists (voiceschanged listener → html.no-tts → .say{display:none}); speechSynthesis.cancel() on visibilitychange-hidden to avoid iOS wedging; static aria-label '読み上げ — play pronunciation'; touch-action:manipulation; -webkit-tap-highlight-color:transparent.",
    "DECLUTTER: v1 ships word audio only (back is already crowded); one-time hint '触れて音 · tap for audio' (11px, first 3 reveals, localStorage key kairo.audioHint); device-voice disclosure lives in Settings, never on-card; future sentence audio reuses the same seal at 16px end-of-line with ::before inset:-14px hit inflation."
  ]
}
```