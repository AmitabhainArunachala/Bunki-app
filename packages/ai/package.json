{
  "name": "@bunki/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded AI candidate path: envelope schema, one provider behind a port, scripted offline fallback.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@bunki/domain": "*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1"
  }
}
