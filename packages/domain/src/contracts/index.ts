/**
 * `RetrievalContract` — the stable scorable unit (WP-06; REQ-DM-05, DL-03).
 *
 * The entity and its validation, the Phase-0 KnowledgeComponent identity, the
 * target→thread projection that gives the gate a promotion state to check, and
 * the promotion→skill activation table.
 */

export * from './component-identity.ts';
export * from './retrieval-contract.ts';
export * from './thread-link.ts';
export * from './promotion-activation.ts';
