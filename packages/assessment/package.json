{
  "name": "@bunki/assessment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Immutable assessment content, explicit review boundaries, attempt checkpoints, and conservative legacy evidence adapters.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run --root ../.. packages/assessment/test",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@bunki/ai": "*",
    "@bunki/domain": "*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1"
  }
}
